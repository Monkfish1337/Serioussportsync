## 0.96.1 — 2026-09-13

- MLB, NFL and NBA give Prowlarr a separate live-search window (default 50 seconds), independent of the first-response discovery wait. Active duplicate searches are shared. Matching late results and account-scoped TorBox availability are retained for refreshes; scheduled preparation stays Bitmagnet-only.
- These promotions aim to return available results after eight seconds and immediately return confirmed playable results while refreshing live discovery. When no pipeline has useful results, the normal request deadline still applies.
- Prowlarr no longer cuts searching off at 70% of its budget. Hash retrieval overlaps searching with at most four download requests at once and shares the full deadline.
- Matching Lab puts past fixtures first, passes real event IDs to preserve specialist metadata, searches Prowlarr within the separate live window, and retains title-only results without hash downloads. Provider counts, durations and incomplete status are visible.
- Existing torrent search scopes are refreshed on demand to activate the new discovery policy. Saved short timing settings are preserved; the separate Prowlarr live window supplies the longer sports search deadline.

Update the SSS container to `ghcr.io/monkfish1337/serioussportsync:v0.96.1`. A refresh may be needed to show results found after the first response. Prowlarr still uses its aggregate indexer search endpoint; per-indexer isolation remains a possible follow-up.
